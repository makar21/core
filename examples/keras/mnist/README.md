# Download dataset

```shell
# Test train local
python add-task.py \
    --local 1 \
    --workers 1 \
    --epochs 1 \
    --batch 128 \
    --dataset examples/keras/mnist \
    --path examples/keras/mnist/mlp.py
```